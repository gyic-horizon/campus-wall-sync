我们在做一个校园墙同步服务，叫 campus-wall-sync，功能是把问卷星收到的投稿自动同步到 Halo 博客上，中间加一层审核（人工或AI）。项目部署在 1Panel 上用 Docker Compose 跑，通过 GitHub Actions 自动发布。但有个关键约束：社团里大多数开发同学不懂 Docker，只懂基础 Python 和 Git，所以代码结构要设计成「业务逻辑和基础设施分离」——他们只需要改 src/hooks/ 下的业务文件（比如敏感词过滤、问卷解析），不用碰 Docker 配置。本地开发要能一键运行（给个 run_local.sh 脚本自动装依赖启动），测试用我们内网那个 Halo 实例。你帮我初始化这个仓库的脚手架，包括 Flask 主入口、配置读取、hooks 目录模板、一键运行脚本，还有给新手看的 README，重点写清楚「开发组只改业务代码、运维组管部署」这个协作分工。代码注释写细一点，适合学生看，密钥配置从 config.json 读别硬编码。你先帮我搭好这个能让人直接上手的框架。